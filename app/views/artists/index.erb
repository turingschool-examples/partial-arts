<%= root_url%>
<div class="jumbotron">
  <h1>Martial Artists</h1>
</div>

<% @artists.each do |artist| %>
  <!-- <--! add something -->
  <%= erb :'/shared/_artist', locals: { artist: artist } %> 
<% end %>
